package models.entities;


public class Professor extends Usuario {
    public Professor(int ID, String type, String name, String username, int password) {
        super(ID, type, name, username, password);
    }
}
