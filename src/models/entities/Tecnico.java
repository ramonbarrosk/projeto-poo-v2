package models.entities;

public class Tecnico extends Usuario {

    public Tecnico(int ID, String type, String name, String username, int password) {
        super(ID, type, name, username, password);
    }
}
